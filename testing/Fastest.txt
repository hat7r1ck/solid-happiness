| makeresults
| eval entity = trim("$tok_entity$")
| where len(entity) > 0
| eval msg = "Investigation entity: " . entity
| table msg


| makeresults
| eval entity = trim("$tok_entity$")
| where len(entity) = 0
| eval msg = "No investigation entity selected. Enter a value in Investigation Entity to start the lookback."
| table msg


| makeresults
| eval host_user = trim("$tok_user_from_hosts$")
| where len(host_user) > 0
| eval msg = "Searching identity records for user: " . host_user
| table msg


| makeresults
| eval host_user = trim("$tok_user_from_hosts$")
| where len(host_user) = 0
| eval msg = "No host linked user selected. Click a user value in Host Details to load identity information."
| table msg
