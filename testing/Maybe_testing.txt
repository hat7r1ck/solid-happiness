then Table.SelectRows(Inc_WithKeys, each List.Contains(OnlyLocations, [Location]))

then Table.SelectRows(
    Inc_WithKeys,
    each [Location] <> null and List.Contains(OnlyLocations, [Location])
)


if [Cases] = null or [Cases] = 0 then null else ...
if [X] = null or [X] = 0 then null else ...
if [X] = null then null else if [X] = 0 then null else ...




each List.Contains(IncludeStatuses, if [Status] = null then "" else [Status])
each [Status] <> null and List.Contains(IncludeStatuses, [Status])


Inc_Safe =
    Table.TransformColumns(
        Inc,
        {
            {"Location", each _, type nullable text},
            {"Status", each _, type nullable text}
        }
    ),
